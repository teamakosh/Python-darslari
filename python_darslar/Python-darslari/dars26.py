matn = "salom"
print(matn.upper())
# bu yerda matn nomli uzgaruvchiga "salom" berilayapti va upper 
#yordamida harflarni kattalshtiilgan

inport random
from uzwords impord words

ddef get_word():
    word=random.choice(words)
    while "-" in word or "" in word:
        word = random.choice(words)
        return word.upper()
    def display(user_letters,word):
        if latter in user_latters.upper():
            display_latter += letter
            else:
                display latter +="-"
        return  display_latter
    def play():
        word=get_word()
        word_latters set(word)
        user_letters=''
        print(fmen {len f"(world)}xonali son uyladim topa olasizmi?"):
            while len(word_latters)>0:
                print (display(user_letters,world))
                if lan(user_latters)>0:
                    print(f"shu vaqtgacha kiritgan harflaringiz:{user_letters}"
                          #bu so'z uyini